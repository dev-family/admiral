import { Locale as FormLocale } from '../form/interfaces';
export declare type FiltersLocale = {
    title: string;
    clear: string;
    submit: string;
};
export declare type Locale = {
    filters: FiltersLocale;
    form: FormLocale;
};
