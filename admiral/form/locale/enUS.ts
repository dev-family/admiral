import { Locale } from '../interfaces'

export const enUS: Locale = {
    fields: {
        array: {
            add: 'Add',
            remove: 'Remove',
        },
    },
    successMessage: 'Data saved successfully!',
}
